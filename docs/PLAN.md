# Статус работ

Метка **← ОСТАНОВИЛИСЬ ЗДЕСЬ** показывает точку продолжения.
Галочка ставится только после того, как пункт реально проверен.

## Блок 0 — инфраструктура

- [x] `.gitignore`
- [x] `package.json`, `index.html`, `vite.config.ts` (с `base: '/portal-lab/'`)
- [x] `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`
- [x] `CLAUDE.md`, `docs/PLAN.md`, `docs/worklog.md`
- [x] GitHub Actions workflow для Pages
- [x] `npm install` — прошёл, 104 пакета, `package-lock.json` создан
- [x] `npm run build` — собирается, `dist/` 264 kB js + 8.5 kB css
- [x] установка GitHub CLI — `winget install GitHub.cli`, версия 2.101.0
- [x] `gh auth login` — аккаунт spisusha
- [x] `git init`, первый коммит, репозиторий github.com/spisusha/portal-lab
- [x] деплой на Pages — https://spisusha.github.io/portal-lab/

## Блок 1 — домен и тесты

- [x] `types.ts` — модель портала, статусы, действия, форматирование времени
- [x] `risk.ts` — формула риска с разбором на слагаемые, ранги E–S
- [x] `rules.ts` — запреты с причинами, двухшаговое закрытие
- [x] `reducer.ts` — переходы состояний, цикл времени, схлопывание
- [x] `recommend.ts` — рекомендуемое действие
- [x] `summary.ts` — счётчики и блок «требуют внимания»
- [x] `seed.ts` — три набора демо-данных
- [x] тесты: `risk.test.ts`, `rules.test.ts`, `reducer.test.ts`, `summary.test.ts`
- [x] **тесты прогнаны** — 24/24 зелёных, 4 файла, ни одной правки в домене

## Блок 2 — интерфейс

- [x] `labStore.tsx` — Context + useReducer
- [x] `theme.css` — тёмная тема, рамки системных окон, цвета рангов
- [x] `SummaryPanel` — счётчики и «требуют внимания»
- [x] `PortalList` + пустое состояние
- [x] `PortalCard` — метрики, разбор риска, рекомендация, действия, история
- [x] `EventLog` — журнал событий
- [x] `ConfirmDialog` — подтверждение опасного закрытия
- [x] `Toolbar` — демо-сценарии и кнопка цикла
- [x] `App` — вкладки «Лаборатория» и «AI Worklog»
- [x] `tsc -b` проходит по всему UI — типы сходятся
- [x] дерево рендерится без runtime-ошибок (разовый прогон через
      `react-dom/server`, файл проверки удалён)
- [x] **прогон в браузере** — все сценарии пройдены автоматизированно
      через CDP (см. «Как проверять работу» в `CLAUDE.md`). Живыми руками
      человек пока не кликал

## Блок 3 — worklog, README, проверка

- [x] `WorklogScreen` — экран AI Worklog внутри приложения
- [ ] подставить реальное время и токены в worklog — **нужны данные от
      человека**, AI их не знает
- [x] ключевые промпты по этапам — **уже есть**, строка «Ключевой промпт»
      в каждом этапе экрана worklog
- [x] `README.md` — ссылка на живую версию проставлена
- [x] скриншот в README
- [x] прогон 5 обязательных состояний — автоматизированно, через браузер
- [x] деплой и проверка ссылки — 200, ассеты 200, приложение
      отрисовывается на https://spisusha.github.io/portal-lab/

## Блок 4 — визуал (день 2)

- [x] переоформление: убран «AI-шный HUD» — свечение, уголки-скобки,
      сплошной моноширинный, CAPS-подписи, радуга рангов
- [x] шкала опасности сведена к одной монотонной линии серый → красный
- [x] появление панелей и диалога — короткая анимация
- [x] пульсация только у ранга S (единственная анимация состояния)
- [x] видимый фокус с клавиатуры, `prefers-reduced-motion`
- [x] узкий экран: на 360 px `scrollWidth` равен ширине окна, переполнений
      нет; строка портала, сводка и слагаемые риска складываются вертикально

## Блок 5 — редизайн: понятность первой минуты (день 3)

Повод: логика работала, но человек, открывший приложение впервые, не понимал,
кто он, какова цель и что нажимать. Первым на экране стоял переключатель
демо-данных, а рекомендация лежала под разбором формулы.

- [x] `domain/focus.ts` — выбор самого опасного портала и текст причины
- [x] `risk.ts` — `riskHeadline` (короткое объяснение) и `RANK_SCALE` (легенда)
- [x] вступление из трёх шагов при первом визите + кнопка «Как это работает»
- [x] постоянная полоса цели смены (не исчезает вместе с модалкой)
- [x] блок «Требует решения сейчас»: кольцо, причина, одна главная кнопка
- [x] новая иерархия экрана: цель → срочное → сводка → список → карточка → журнал
- [x] вычисления риска убраны под `<details>`, сверху человеческая фраза
- [x] рекомендованное действие помечено и в карточке
- [x] легенда рангов E–S (раньше шкала была без объяснения)
- [x] дизайн-система «чернила и нефрит», палитра проверена на контраст
- [x] девять авторских SVG-сцен миров + мелкие знаки в списке
- [x] кольцо портала: шкала опасности формой, а не только цветом
- [x] микроанимации: вращение кольца, дыхание ядра, дрейф частиц,
      плавные показатели, подсветка свежей записи журнала
- [x] `prefers-reduced-motion` гасит всё движение
- [x] 12 интеграционных UI-тестов (Testing Library + jsdom)
- [x] контраст: 23 пары цветов, все ≥ 4.5:1
- [x] 360 / 390 / 768 / 1280 px — горизонтальной прокрутки нет
- [x] ошибок в консоли нет
- [x] задеплоено: хеши файлов на Pages совпадают с локальной сборкой,
      живая ссылка пройдена браузером начисто (вступление показывается)
- [x] правки по развёрнутому пользовательскому аудиту интерфейса

## Блок 6 — финальная переделка интерфейса

- [x] центральная камера: крупный портал, ранг, риск, метрики, угроза,
      рекомендация и прогноз выбранного портала
- [x] очередь сортируется по опасности, критические состояния читаются не
      только по цвету
- [x] панель четырёх решений показывает последствия и причины запретов
- [x] перед следующим циклом виден прогноз; критический портал не блокирует
      демонстрационный запуск цикла
- [x] после действия видна разница «до → после», журнал остаётся архивом
- [x] формула риска, история и журнал убраны в раскрывающиеся панели
- [x] онбординг сокращён до роли, ориентира и первого действия с подсветкой
- [x] AI Worklog: читаемая ширина, содержание, сводка, этапы и раскрытия
- [x] Playwright: все обязательные сценарии, клавиатура, консоль без ошибок
- [x] 1440×900 / 768×1024 / 390×844 — горизонтального скролла нет
- [x] скриншоты «до» и «после» сохранены в `.screenshots/`
- [x] `npm test` — 55/55; `npm run build` — успешно

## Что ещё не закрыто по ТЗ

- [ ] **время и токены в worklog** — плейсхолдеры на месте, данных у AI нет.
      Время знает только человек, токены показывает `/cost` в Claude Code.
- [ ] **«сценарий понятен за минуту»** — единственный пункт чеклиста, который
      нельзя проверить автоматически: нужен человек, впервые видящий экран.

---

## Примечание о блокировке — снято

Блокировка установки действовала только в сессии дня 1. В сессии дня 2
`npm install`, `npm test` и `npm run build` отработали без помех.

Настоящий путь проекта (в старой записи был указан неверно):

```sh
cd "C:\Users\spice\Desktop\Projects\portal-lab"
npm install
npm test        # 24/24
npm run build   # dist/
npm run dev     # http://localhost:5173/portal-lab/
```

Что осталось непроверенным: клики в браузере, GitHub Pages (репозиторий
ещё не создан, `git init` не делался).

---

## Блок 7 — версия 1.1 «Живая смена» (ветка `feature/live-shifts-v1.1`)

Ветка создана от тега `v1.0.0` (`772e154`). `main`, тег и опубликованный
сайт не изменялись; Pages по-прежнему публикуется только из `main`.

### Домен

- [x] `live/prng.ts` — детерминированный PRNG от строки seed (xmur3 + mulberry32)
- [x] `live/seedCode.ts` — код `PL-XXXX` без символов, которые путаются
- [x] `live/url.ts` — `?mode=live&seed=…` с учётом base path `/portal-lab/`
- [x] `live/worlds.ts` — по одной особенности на каждый из девяти миров
- [x] `live/events.ts` — семь типов событий, не больше одного на переход
- [x] `live/directives.ts` — шесть директив + проверка выполнимости на старте
- [x] `live/generator.ts` — пять порталов по ролям, а не отбраковкой
- [x] `live/score.ts` — счёт из четырёх блоков, ранги S–D, научные данные
- [x] `live/debrief.ts` — хроника решений без оценок и контрфактики
- [x] `simulate.ts` — весь переход целиком: общая точка редьюсера и прогноза
- [x] `plural.ts` — числительные переехали из `ui/` в домен
- [x] `Portal.trait`, `ScenarioId: 'live'`, `LabState.live` — у демо пусто

### Интерфейс

- [x] переключатель «Режим смены»: три прежних набора + «Живая смена»
- [x] код смены мелкой подписью, он же в адресной строке
- [x] особенность мира у названия мира — в камере и в строке очереди
- [x] директива компактной строкой у прогресса смены
- [x] событие в существующем блоке «Через цикл» и в журнале, без модалок
- [x] `LiveComplete` — счёт, ранг буквой/словом/числом, разбор решений
- [x] три действия: повторить, новая, поделиться + текст «Ссылка скопирована»
- [x] отдельных панелей под новые механики не заведено, редизайна нет

### Проверено

- [x] `npm ci`, `npm test` — 165/165, `npm run build` — успешно
- [x] локальный production preview + ручной прогон в Chrome
- [x] создание живой смены, открытие той же смены по ссылке
- [x] особенности миров видны до решения и применяются как написано
- [x] прогноз события совпадает с фактом (сравниваются состояния, не тексты)
- [x] выполненная директива (`PL-7K42`) и невыполненная (`PL-LAB2`)
- [x] итоговый рейтинг, повтор смены, новая смена, копирование ссылки
- [x] старые Standard, Critical и Empty — поведение не изменилось
- [x] AI Worklog открывается, навигация и разделы на месте
- [x] 1440 / 768 / 390 / 360 px — горизонтального переполнения нет
- [x] клавиатура, видимый фокус, Escape, `prefers-reduced-motion`
- [x] консоль без ошибок и предупреждений
- [x] версия поднята до 1.1.0 в `package.json` и `package-lock.json`

### Найдено глазами, а не тестами

- [x] полоса цели смены уезжала под архив с версии 1.0 (у `.shift-progress`
      не было `grid-area`) — исправлено вместе с размещением директивы
- [x] «Спасено 1 существ» — числительные не доходили до домена
- [x] заголовок итога сортировал блоки по долям, а обещал абсолютные очки
- [x] `clipboard.writeText` мог не завершиться — кнопка молчала

### Не закрыто на момент версии 1.1

- [x] **время и токены в worklog** — цифры получены от пользователя
      и проставлены в `docs/worklog.md` и на экране
- [ ] **«сценарий понятен за минуту»** — нужен человек, впервые видящий экран
      (перешло из версии 1.0)

---

## Блок 8 — финальный проход перед публикацией

### Тексты

- [x] второй шаг onboarding: полная шкала `E–S` вместо `S / A / B`,
      ключ хранения поднят до `v4`, авто-показ проверен на чистом состоянии
- [x] прогноз «Через цикл» переписан: «В следующем цикле ни один портал
      не схлопнется. Сильнее всего ухудшится состояние …»
- [x] остальные ветки прогноза: одно схлопывание, несколько, пустая
      лаборатория, завершённая смена, событие живой смены, после стабилизации
- [x] числительные в прогнозе: `портал / портала / порталов`,
      `существо / существа / существ`
- [x] AI Worklog заменён утверждённым текстом; `docs/worklog.md` совпадает
      с экраном слово в слово по содержанию

### Разделение режимов

- [x] `optgroup` в переключателе: «Учебные сценарии» и «Основной режим»
- [x] карточка режима под переключателем: метка, название, объяснение,
      действие; меняется вместе с выбором
- [x] у трёх учебных сценариев — кнопка «Перейти в живую смену»
- [x] у живой смены — код смены в заголовке и запуск таймера

### Живое время (только «Живая смена»)

- [x] `state/liveClock.tsx` — единственный источник реального времени;
      в домене по-прежнему нет ни `Date.now`, ни `setInterval`
- [x] 15 лабораторных минут за 60 реальных секунд, часы идут поминутно
- [x] отсчёт «До следующего цикла · 00:43» рядом с прогнозом
- [x] запуск только по кнопке «Начать живую смену»
- [x] `Пауза` / `Продолжить` и вторичная «Завершить цикл сейчас»
- [x] автоматическая пауза: onboarding, подтверждение, вкладка Worklog,
      скрытая вкладка, конец смены
- [x] пропущенное в скрытой вкладке время не применяется
- [x] один переход на цикл: одновременное нажатие и истечение отсчёта
      не дают двойного цикла
- [x] учебные сценарии сохранили кнопку «Следующий цикл · +15 мин»
- [x] 16 тестов на поддельных таймерах (`ui/__tests__/liveClock.test.tsx`)

### Проверено

- [x] `npm test` — 195/195 в 15 файлах, `npm run build` — успешно
- [x] production preview в браузере: учебные сценарии, живая смена,
      таймер, пауза, завершение, итог со счётом
- [x] 1440 / 768 / 390 / 360 px — горизонтального переполнения нет
- [x] клавиатура доходит до переключателя, запуска и паузы
- [x] `prefers-reduced-motion` гасит пульсацию, но не останавливает время
- [x] консоль без ошибок и предупреждений

### Найдено глазами, а не тестами

- [x] адаптив карточки режима и отсчёта стоял в общем блоке «Адаптив»
      выше по файлу и при равной специфичности проигрывал базовым
      правилам: на 768 px карточка не растягивалась
- [x] на 390 px строка состояния таймера растягивалась по высоте колонки
      и отрывала кнопку «Пауза» от текста
- [x] разбор формулы риска на 360 px обрезал колонку арифметики
      («0.35 × 75 = 26»): страница не переполнялась, поэтому измерение
      `scrollWidth` молчало. На телефоне строка таблицы развёрнута в три

### Осталось

- [ ] merge в `main`, публикация на Pages ← ОСТАНОВИЛИСЬ ЗДЕСЬ
