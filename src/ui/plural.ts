/**
 * Числительные переехали в домен: их использует и интерфейс, и текст
 * объяснений, который собирает `domain/live/score.ts`. Реэкспорт оставлен,
 * чтобы компоненты не знали о переезде.
 */
export { counted, plural } from '../domain/plural'
